plugins {
    id 'java-library'
}

repositories {
    mavenCentral()
}

//техническое для сокращения лога запуска теста
def selenideVersion = "6.2.1",
    junitVersion = "5.8.2",
    slf4jVersion = "1.7.32"


dependencies {
    testImplementation(
            'com.codeborne:selenide:6.2.1',
            'org.junit.jupiter:junit-jupiter:5.8.2'
    )
    testRuntimeOnly('org.junit.jupiter:junit-jupiter-engine:5.8.2',
            "org.slf4j:slf4j-simple:1.7.32") //техническое для сокращения лога запуска теста
}

//fix: кириллица
//tasks.withType(JavaCompile) {
//    options.encoding = 'UTF-8'
//}

test {
    useJUnitPlatform()
}